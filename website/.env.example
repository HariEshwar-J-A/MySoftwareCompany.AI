# Website env vars live in the repo-root file (single source of truth):
#   ../.env.example  → copy to ../.env
#
# Next.js loads ../.env automatically via next.config.ts when you run npm run dev here.
